services:
  postgres:
    container_name: postgres_go
    image: postgres:16.4
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 696969
      PGDATA: /data/postgres
    volumes:
      - ./dbfiles:/data/postgres
    ports:
      - "5432:5432"